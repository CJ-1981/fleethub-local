diff --git a/index.html b/index.html
index 2d405a8..da88860 100644
--- a/index.html
+++ b/index.html
@@ -552,8 +552,11 @@ function importJSONToSQLite(jsonData) {
     });
   }
   if (jsonData.settings) {
-    for (const k of Object.keys(DEFAULT_SETTINGS)) {
-      if (jsonData.settings[k] !== undefined) setSetting(k, jsonData.settings[k]);
+    // First apply defaults for any missing keys
+    initDefaultSettings();
+    // Then import ALL settings from the JSON (including userNameConfig, archiveEnabled, etc.)
+    for (const k of Object.keys(jsonData.settings)) {
+      setSetting(k, jsonData.settings[k]);
     }
   }
   if (jsonData.meta) {
@@ -808,7 +811,8 @@ async function openFile() {
       const text = await file.text();
       importJSONToSQLite(JSON.parse(text));
     } else {
-      try { db = new sqlJsModule.Database(new Uint8Array(arrayBuffer)); } catch(e) { createDatabase(); }
+      try { db = new sqlJsModule.Database(new Uint8Array(arrayBuffer)); }
+      catch(e) { showToast('Could not open database — file may be corrupt or unsupported. Starting with a blank database.', 'warning'); createDatabase(); initDefaultSettings(); }
     }
     saveRecentFile(handle.name);
     enterApp();
@@ -946,7 +950,7 @@ function renderRecentFiles() {
 }
 
 // ===================== LOCKING =====================
-function getMyLockId() { return localStorage.getItem('fleethub_lockid') || uid(); }
+function getMyLockId() { return S._lockId || (S._lockId = uid()); }
 
 function initLock() {
   S.lockId = getMyLockId();
@@ -1943,7 +1947,43 @@ function renderShareTags() {
 
 function openShareDialog(id) {
   const b = getBookings().find(b => b.id === id);
-  if (b) { S.shareDialogBooking = b; openDialog('Share Ride', renderShareTags(), null); }
+  if (!b) return;
+  S.shareDialogBooking = b;
+  const tags = (b.shareWith || []).map(name => `<span class="share-tag">${esc(name)}<button data-name="${escAttr(name)}" onclick="removeShareDialogName(this,this.dataset.name)">×</button></span>`).join('');
+  const html = `<div class="share-panel">
+    <p style="font-size:13px;margin-bottom:8px">${esc(b.userName)} — ${esc(b.purpose)}</p>
+    <div class="share-tags" id="share-dialog-tags">${tags || '<span style="font-size:13px;color:var(--muted)">No co-passengers</span>'}</div>
+    <div style="display:flex;gap:8px;margin-top:8px">
+      <input class="form-input" id="share-dialog-input" placeholder="Add co-passenger name" style="flex:1" onkeydown="if(event.key==='Enter'){event.preventDefault();addShareDialogName()}">
+      <button class="btn btn-sm" onclick="addShareDialogName()">Add</button>
+    </div>
+    <div class="form-group" style="margin-top:8px;margin-bottom:0"><label class="form-label">Share Note</label><textarea class="form-input" id="share-dialog-note" rows="2" placeholder="Optional note...">${esc(b.shareNote || '')}</textarea></div>
+  </div>`;
+  openDialog('Share Ride', html, () => {
+    db.run("UPDATE bookings SET share_with=?, share_note=?, updated_at=? WHERE id=?",
+      [JSON.stringify(S.shareDialogBooking.shareWith || []), document.getElementById('share-dialog-note').value.trim() || null, new Date().toISOString(), b.id]);
+    markDirty(); render(); showToast('Share details updated', 'success'); return true;
+  });
+}
+
+function addShareDialogName() {
+  const input = document.getElementById('share-dialog-input');
+  const name = input.value.trim();
+  if (!name || !S.shareDialogBooking) return;
+  if (!S.shareDialogBooking.shareWith) S.shareDialogBooking.shareWith = [];
+  if (!S.shareDialogBooking.shareWith.includes(name)) {
+    S.shareDialogBooking.shareWith.push(name);
+    const tagsEl = document.getElementById('share-dialog-tags');
+    if (tagsEl) tagsEl.innerHTML = (S.shareDialogBooking.shareWith || []).map(n => `<span class="share-tag">${esc(n)}<button data-name="${escAttr(n)}" onclick="removeShareDialogName(this,this.dataset.name)">×</button></span>`).join('');
+  }
+  input.value = '';
+}
+
+function removeShareDialogName(btn, name) {
+  if (!S.shareDialogBooking) return;
+  S.shareDialogBooking.shareWith = (S.shareDialogBooking.shareWith || []).filter(n => n !== name);
+  const tagsEl = document.getElementById('share-dialog-tags');
+  if (tagsEl) tagsEl.innerHTML = (S.shareDialogBooking.shareWith || []).map(n => `<span class="share-tag">${esc(n)}<button data-name="${escAttr(n)}" onclick="removeShareDialogName(this,this.dataset.name)">×</button></span>`).join('') || '<span style="font-size:13px;color:var(--muted)">No co-passengers</span>';
 }
 
 // Legacy reference
@@ -2478,13 +2518,20 @@ async function sendChat() {
   renderChat();
 }
 
-const AI_TOOLS = [
-  { type: 'function', function: { name: 'create_booking', description: 'Create a new vehicle booking.', parameters: { type: 'object', properties: { vehicleName: { type: 'string', description: 'Vehicle name' }, userName: { type: 'string', description: 'Name of the person' }, purpose: { type: 'string', description: 'Purpose' }, periodStart: { type: 'string', description: 'YYYY-MM-DD' }, periodEnd: { type: 'string', description: 'YYYY-MM-DD' }, status: { type: 'string', enum: ['pending','confirmed','rejected','cancelled'] } }, required: ['vehicleName','userName','purpose','periodStart','periodEnd'] } } },
-  { type: 'function', function: { name: 'update_vehicle_status', description: 'Change a vehicle status.', parameters: { type: 'object', properties: { vehicleName: { type: 'string' }, status: { type: 'string', enum: ['available','in-use','maintenance','out-of-service'] } }, required: ['vehicleName','status'] } } },
-  { type: 'function', function: { name: 'cancel_booking', description: 'Cancel a booking by ID.', parameters: { type: 'object', properties: { bookingId: { type: 'string' } }, required: ['bookingId'] } } },
-  { type: 'function', function: { name: 'list_vehicles', description: 'List all vehicles.', parameters: { type: 'object', properties: {} } } },
-  { type: 'function', function: { name: 'list_bookings', description: 'List bookings.', parameters: { type: 'object', properties: { status: { type: 'string' } } } } }
-];
+function getAITools() {
+  const statuses = getSetting('statuses', ['pending','confirmed','in-use','returned','cancelled']);
+  const vehicleTypes = getSetting('vehicleTypes', ['sedan','suv','pickup','van','bus','motorcycle','other']);
+  return [
+    { type: 'function', function: { name: 'create_booking', description: 'Create a new vehicle booking.', parameters: { type: 'object', properties: { vehicleName: { type: 'string', description: 'Vehicle name' }, userName: { type: 'string', description: 'Name of the person' }, purpose: { type: 'string', description: 'Purpose' }, periodStart: { type: 'string', description: 'YYYY-MM-DD' }, periodEnd: { type: 'string', description: 'YYYY-MM-DD' }, status: { type: 'string', enum: statuses } }, required: ['vehicleName','userName','purpose','periodStart','periodEnd'] } } },
+    { type: 'function', function: { name: 'update_vehicle_status', description: 'Change a vehicle status.', parameters: { type: 'object', properties: { vehicleName: { type: 'string' }, status: { type: 'string', enum: vehicleTypes } }, required: ['vehicleName','status'] } } },
+    { type: 'function', function: { name: 'cancel_booking', description: 'Cancel a booking by ID.', parameters: { type: 'object', properties: { bookingId: { type: 'string' } }, required: ['bookingId'] } } },
+    { type: 'function', function: { name: 'list_vehicles', description: 'List all vehicles.', parameters: { type: 'object', properties: {} } } },
+    { type: 'function', function: { name: 'list_bookings', description: 'List bookings.', parameters: { type: 'object', properties: { status: { type: 'string' } } } } }
+  ];
+}
+
+// Keep AI_TOOLS as fallback for legacy references
+const AI_TOOLS = getAITools();
 
 function executeToolCall(name, args) {
   switch (name) {
@@ -2537,7 +2584,7 @@ async function callAIWithTools(apiEndpoint, apiKey, userText) {
   ];
   const res = await fetch(apiEndpoint, {
     method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${apiKey}` },
-    body: JSON.stringify({ model, messages, tools: AI_TOOLS, tool_choice: 'auto' })
+    body: JSON.stringify({ model, messages, tools: getAITools(), tool_choice: 'auto' })
   });
   if (!res.ok) {
     const res2 = await fetch(apiEndpoint, {
@@ -2562,7 +2609,7 @@ async function callAIWithTools(apiEndpoint, apiKey, userText) {
   }
   const res2 = await fetch(apiEndpoint, {
     method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${apiKey}` },
-    body: JSON.stringify({ model, messages, tools: AI_TOOLS })
+    body: JSON.stringify({ model, messages, tools: getAITools() })
   });
   if (!res2.ok) throw new Error(`API error ${res2.status}`);
   const d2 = await res2.json();
